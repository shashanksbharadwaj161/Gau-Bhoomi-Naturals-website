DirectoryIndex index.html index.php

Options -MultiViews
RewriteEngine On
RewriteBase /

<Files wp-config.php>
  order allow,deny
  deny from all
</Files>

# WordPress admin — serve directly
RewriteRule ^wp-admin(/.*)?$ - [L]
RewriteRule ^wp-login\.php$ - [L]

# WordPress REST API — MUST route through WordPress index.php
# (not served as static file — that causes 404)
RewriteRule ^wp-json(/.*)?$ /index.php [QSA,L]

# WordPress static files — serve directly
RewriteRule ^wp-content(/.*)?$ - [L]
RewriteRule ^wp-includes(/.*)?$ - [L]
RewriteRule ^wp-cron\.php$ - [L]
RewriteRule ^xmlrpc\.php$ - [L]

# React build assets — serve directly
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Everything else → React SPA
RewriteRule ^ /index.html [QSA,L]

<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
</IfModule>

<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css application/javascript
</IfModule>
